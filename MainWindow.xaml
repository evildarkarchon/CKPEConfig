<Window x:Class="CKPEConfig.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CreationKit Platform Extended INI Editor" 
        Height="600" Width="800"
        MinHeight="600" MinWidth="800"
        Background="{StaticResource WindowBackgroundBrush}">
    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid x:Name="MainContent" Grid.Row="0" Background="{StaticResource WindowBackgroundBrush}">
            <!-- This will be populated with either branding or tab control -->
        </Grid>

        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="10"
                    Background="Transparent">
            <Button Content="Load INI" Click="LoadIni_Click" />
            <Button Content="Save INI" Click="SaveIni_Click" />
        </StackPanel>
    </Grid>
</Window>